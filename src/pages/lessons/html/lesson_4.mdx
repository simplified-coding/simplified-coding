---
---

<h1 style="text-align: center;"> Μάθημα 4 </h1>

<h2 style="text-align: center;"> Μορφοποίηση κειμένου HTML </h2>

---

<h2 style="text-align: center;"> `b` element </h2>

Το ```<b>``` κάνει ένα κομμάτι κειμένου να εμφανίζεται σε έντονη μορφή (bold).

Παράδειγμα:

```
<p> Αυτό είναι <b>ένα κομμάτι κειμένου</b> σε έντονη μορφή. </p>
```

Αποτέλεσμα:

<p> Αυτό είναι <b> ένα κομμάτι κειμένου </b> σε έντονη μορφή. </p>

---

<h2 style="text-align: center;"> `i` element </h2>

Το ```<i>``` κάνει ένα κομμάτι κειμένου να εμφανίζεται σε πλάγια μορφή (italic).

Παράδειγμα:

```
<p> Αυτό είναι <i>ένα κομμάτι κειμένου</i> σε πλάγια μορφή. </p>
```

Αποτέλεσμα:

```html
<p> Αυτό είναι <i>ένα κομμάτι κειμένου</i> σε πλάγια μορφή. </p>
```

---

<h2 style="text-align: center;"> `u` element </h2>

Το ```<u>``` κάνει ένα κομμάτι κειμένου να εμφανίζεται με υπογράμμιση.

Παράδειγμα:

```
<p>Αυτό είναι <u>ένα κομμάτι κειμένου</u> με υπογράμμιση.</p>
```

Αποτέλεσμα:
```html
<p> Αυτό είναι <u>ένα κομμάτι κειμένου</u> με υπογράμμιση. </p>
```

---

<h2 style="text-align: center;"> `mark` element </h2>

Το ```<mark>``` κάνει ένα κομμάτι κειμένου να εμφανίζεται με χρώμα φόντου (highlighted).

Παράδειγμα:

```
<p>Αυτό είναι <mark>ένα κομμάτι κειμένου</mark> με χρώμα φόντου.</p>
```

Αποτέλεσμα:
```html
<p>Αυτό είναι <mark>ένα κομμάτι κειμένου</mark> με χρώμα φόντου.</p>
```

---

<h2 style="text-align: center;"> `small` element </h2>

Το ```<small>``` κάνει ένα κομμάτι κειμένου να εμφανίζεται με μικρότερη γραμματοσειρά.

Παράδειγμα:

```
<p> Αυτό είναι <small>ένα κομμάτι κειμένου</small> με μικρότερη γραμματοσειρά.</p>
```

Αποτέλεσμα:
```html
<p> Αυτό είναι <small>ένα κομμάτι κειμένου</small> με μικρότερη γραμματοσειρά.</p>
```

---

<h2 style="text-align: center;"> `del` element </h2>

Το ```<del>``` κάνει ένα κομμάτι κειμένου να εμφανίζεται με διαγραμμένο κείμενο.

Παράδειγμα:

```
<p>Αυτό είναι <del>ένα κομμάτι κειμένου</del> με διαγραμμένο κείμενο.</p>
```

Αποτέλεσμα:

```html
<p>Αυτό είναι <del>ένα κομμάτι κειμένου</del> με διαγραμμένο κείμενο.</p>
```

---

<h3 style="text-align: center;"> Ευχαριστούμε για την παρακολούθηση του μαθήματος. </h3>